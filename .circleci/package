#!/bin/bash

pwd
echo HOME is $HOME

find . -ls

echo $CPUS

export PATH="$PATH:/home/build/flutter/bin:/home/build/.pub-cache/bin"

# cd boatinstrument
# ./package "$@"
# mv packages/* /tmp/artifacts
